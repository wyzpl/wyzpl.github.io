{
  "name": "rspress-doc-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "deploy": "rspress build && git add . && git commit -m 'deploy' && git push"
  },
  "dependencies": {
    "@rspress/shared": "^1.25.1",
    "rspress": "^1.18.0",
    "rspress-plugin-align-image": "^0.3.0",
    "rspress-plugin-google-analytics": "^0.3.0",
    "scss": "^0.2.4"
  },
  "devDependencies": {
    "@types/node": "^20"
  }
}
