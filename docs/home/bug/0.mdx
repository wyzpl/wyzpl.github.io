# UniApp 编译 APP 中 video 黑屏

### 问题描述

在 UniApp 中使用 video 组件，在编译 APP 后，在`u-popup`中无法正常显示视频,会出现黑屏。

### 问题排查

在页面中通过硬编码测试，使用静态视频地址和后端返回的地址都可以正常显示和播放视频，但在`u-popup`无法正常显示，确定了问题原因

### 解决方案

> 暂未找到可以在`u-popup`中正常显示 video 组件的解决方案

而是通过改变交互的方式来解决问题：点击播放视频时，额外弹出一个视频详情进行自动播放
